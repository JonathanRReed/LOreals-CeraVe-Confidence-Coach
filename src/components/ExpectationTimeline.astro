---
import { milestones } from '../lib/mock';
---

<section class="py-12 bg-gradient-to-b from-white to-gray-50">
  <div class="container mx-auto px-4">
    <h2 class="text-3xl font-bold text-center text-cerave-blue mb-3">
      What to Expect on Your Journey
    </h2>
    <p class="text-center text-gray-600 mb-12 max-w-2xl mx-auto">
      Skincare takes time. Here's a realistic timeline to help you stay patient and consistent.
    </p>

    <div class="relative">
      <div class="hidden md:block absolute top-1/2 left-0 right-0 h-1 bg-cerave-light-blue transform -translate-y-1/2" aria-hidden="true"></div>
      
      <div class="grid grid-cols-1 md:grid-cols-5 gap-8 md:gap-4 relative">
        {
          milestones.map((milestone, index) => (
            <div class="flex flex-col items-center text-center">
              <div class="w-16 h-16 rounded-full bg-cerave-blue text-white flex items-center justify-center font-bold mb-4 relative z-10 shadow-lg">
                Day
                <br />
                {milestone.day}
              </div>
              <h3 class="font-semibold text-gray-900 mb-2">{milestone.title}</h3>
              <p class="text-sm text-gray-600 leading-relaxed">{milestone.note}</p>
            </div>
          ))
        }
      </div>
    </div>

    <div class="mt-12 p-6 bg-blue-50 border border-cerave-light-blue rounded-lg max-w-3xl mx-auto">
      <p class="text-sm text-gray-700 leading-relaxed">
        <strong class="text-cerave-blue">Remember:</strong> Everyone's skin is different.
        These timelines are guidelines, not guarantees. If you experience persistent irritation,
        redness, or discomfort, stop use and consult a dermatologist.
      </p>
    </div>
  </div>
</section>
