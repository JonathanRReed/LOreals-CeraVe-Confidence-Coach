---
const testimonials = [
  {
    text: "This tool gave me confidence to start my skincare journey. The patch test guide was so helpful!",
    name: "Sarah M.",
    age: 24,
    concern: "Acne-prone skin"
  },
  {
    text: "Finally, a routine that actually makes sense for my sensitive skin. Love the personalized approach.",
    name: "James K.",
    age: 32,
    concern: "Sensitive skin"
  },
  {
    text: "The timeline helped me stay patient. I can already see improvements after 30 days!",
    name: "Maria L.",
    age: 28,
    concern: "Dark spots"
  }
];
---

<section class="py-12 bg-white">
  <div class="container mx-auto px-4 max-w-6xl">
    <h3 class="text-2xl font-bold text-center text-cerave-blue mb-8">What People Are Saying</h3>
    <div class="grid md:grid-cols-3 gap-6">
      {testimonials.map((testimonial, idx) => (
        <div class={`p-6 bg-gradient-to-br from-gray-50 to-white rounded-xl border border-gray-200 hover:border-cerave-blue/30 hover:shadow-lg transition-all duration-300 hover:-translate-y-1 animate-fadeIn delay-${idx * 100}`}>
          <div class="flex mb-3 gap-1">
            {[1,2,3,4,5].map((star) => (
              <svg key={star} class="w-4 h-4 text-yellow-400 fill-current transform hover:scale-125 transition-transform" viewBox="0 0 20 20">
                <path d="M10 15l-5.878 3.09 1.123-6.545L.489 6.91l6.572-.955L10 0l2.939 5.955 6.572.955-4.756 4.635 1.123 6.545z"/>
              </svg>
            ))}
          </div>
          <p class="text-sm text-gray-700 mb-4 italic leading-relaxed">"{testimonial.text}"</p>
          <div class="text-xs text-gray-600">
            <p class="font-semibold text-cerave-blue">{testimonial.name}, {testimonial.age}</p>
            <p class="text-gray-500">{testimonial.concern}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
